  <div class="field">
    <%= simple_form_for([@cocktail, @dose]) do |f| %>

      <%= f.association :ingredient %>
      <%= f.input :amount %>
      <%= f.input :description %>
      <%= f.button :submit %>
    <% end %>
  </div>

